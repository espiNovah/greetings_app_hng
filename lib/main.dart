import 'package:flutter/material.dart';
import 'package:greetings_app_hng/startup.dart';

void main() {
  runApp(MyApp());
}

class MyApp extends StatelessWidget {
  // This widget is the root of your application.
  @override
  Widget build(BuildContext context) {
    return Startup();
  }
}
